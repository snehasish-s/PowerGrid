package com.tpcodl.powerpulse;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.ActiveProfiles;

/**
 * Basic smoke test — verifies the Spring application context loads.
 */
@SpringBootTest
class PowerPulseApplicationTests {

    @Test
    void contextLoads() {
        // Verifies all beans and configurations wire up correctly
    }
}
